# Py Notes

### py run commnad
py (root foldername) /filename
## py problem
ctrl+shift+m
## note
use triple quotes,to have long message in a variablejj
"""
msg
"""
built in function
1.len-to get len of string
us parenthesis to call an function
eg len(course) ->course variable

2.index var[0] -->[] holds the value of indexs
[0:3] -->takes index of given range of index
if end is not given for an index it will go till the end of the string.
if start and end is not given  it will return copy of original st

3.strip()- used to trim the white space in the starting and end of the  given string.

4.find()-used to find the given sring in the var
